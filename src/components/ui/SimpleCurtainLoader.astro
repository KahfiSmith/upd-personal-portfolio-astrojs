---
// Simple Curtain Loader - Hanya Brand Name
---

<div
  id="simple-curtain-loader"
  class="fixed inset-0 z-[9999] overflow-hidden should-show"
  style="position:fixed;inset:0;z-index:9999;background:#0b0b0b;opacity:1;"
>
  
  <!-- Panel Atas (hitam solid) -->
  <div class="curtain-top absolute top-0 left-0 w-full h-1/2 bg-neutral-950 z-10"></div>
  
  <!-- Panel Bawah (hitam solid) -->
  <div class="curtain-bottom absolute bottom-0 left-0 w-full h-1/2 bg-neutral-950 z-10"></div>

  <!-- Brand Content di tengah -->
  <div class="loader-center absolute inset-0 flex items-center justify-center z-20">
    <div class="text-center">
      
      <!-- Simple Brand Display -->
      <div class="brand-container">
        <h1 class="brand-name text-7xl md:text-9xl font-semibold text-white mb-6 tracking-wider opacity-0">
          Kahfi Smith
        </h1>
        <p class="brand-subtitle text-neutral-300 text-xl md:text-2xl tracking-[0.3em] uppercase opacity-0">
          Portfolio
        </p>
        
        <!-- Progress Container -->
        <div class="progress-container mt-12 opacity-0">
          <!-- Progress Bar -->
          <div class="progress-bar-container relative w-64 h-1 bg-neutral-700/30 rounded-full mx-auto mb-4">
            <div class="progress-bar absolute top-0 left-0 h-full bg-white rounded-full" style="width: 0%;"></div>
          </div>
          
          <!-- Progress Percentage -->
          <div class="progress-text text-white text-lg font-mono tracking-wider">
            <span class="progress-number">0</span>%
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Minimal decorative background -->
  <div class="decorative-bg absolute inset-0 z-0">
    <!-- Subtle grid pattern -->
    <div class="grid-pattern absolute inset-0 opacity-[0.01]" style="background-image: radial-gradient(circle, white 1px, transparent 1px); background-size: 60px 60px;"></div>
    <!-- Single floating orb -->
    <div class="floating-orb absolute top-1/2 left-1/2 w-96 h-96 bg-gradient-to-r from-blue-500/5 to-purple-500/5 rounded-full blur-3xl transform -translate-x-1/2 -translate-y-1/2"></div>
  </div>

</div>

<style>
  /* Font styling */
  .brand-name {
    font-family: var(--font-display, 'Playfair Display', serif);
    text-shadow: 0 0 40px rgba(255, 255, 255, 0.3);
  }
  
  .brand-subtitle {
    font-family: var(--font-mono, 'Space Grotesk', monospace);
    font-weight: 300;
  }

  /* Smooth transforms */
  .curtain-top,
  .curtain-bottom,
  .brand-name,
  .brand-subtitle,
  .brand-line {
    will-change: transform, opacity;
    backface-visibility: hidden;
  }

  /* Subtle floating animation */
  .floating-orb {
    animation: subtleFloat 8s ease-in-out infinite;
  }

  @keyframes subtleFloat {
    0%, 100% { 
      transform: translate(-50%, -50%) scale(1); 
    }
    50% { 
      transform: translate(-50%, -45%) scale(1.05); 
    }
  }

  /* Hidden by default; only show on reload */
  #simple-curtain-loader { display: none; }
  html.is-reload #simple-curtain-loader { display: block; }
  /* If first-visit reveal is active, always hide this loader to avoid clashes */
  html.needs-reveal #simple-curtain-loader { display: none !important; }
  /* During Astro view transitions, avoid showing page-level loader */
  html[data-astro-transition] #simple-curtain-loader { display: none !important; }
</style>

<noscript>
  <style>
    /* If JS is disabled, hide loader so content is accessible */
    #simple-curtain-loader { display: none !important; }
  </style>
  
</noscript>
