---
const { name, level } = Astro.props as { name: string; level?: string };
---
<span style="display:inline-flex; align-items:center; gap:6px; font-size:.9rem; padding:6px 10px; border:1px solid #eee; border-radius:999px">
  {name}
  {level && <small style="color:#666">({level})</small>}
</span>

