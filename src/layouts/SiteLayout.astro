---
import "../styles/global.css";
import CustomCursor from "@/components/ui/CustomCursor.astro";
import bootAnimations from "@/scripts/boot-animations.ts?url";
import smoothScroll from "@/lib/interactions/smooth-scroll.ts?url";

interface Props {
  title?: string;
  description?: string;
}

const { description } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/images/kahfi-og.png" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Kahfi Smith</title>
    {description && <meta name="description" content={description} />}
    <meta property="og:title" content="Kahfi Smith" />
    <meta property="og:image" content="/images/kahfi-og.png" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Kahfi Smith" />
    <meta name="twitter:image" content="/images/kahfi-og.png" />
  </head>
  <body class="bg-cream text-charcoal antialiased min-h-screen">
    <CustomCursor />
    <div id="page-transition-overlay" class="fixed inset-0 z-50 bg-neutral-950 pointer-events-none transform scale-y-0 origin-top"></div>
    <noscript><style>#page-transition-overlay{display:none !important}</style></noscript>

    <slot />

    <div class="fixed bottom-4 right-4 z-50 flex items-center gap-2" aria-hidden="true">
      <span class="text-xs text-neutral-400">GSAP</span>
      <div data-gsap-test class="h-3 w-3 rounded-full bg-emerald-400 shadow-[0_0_12px_#34d399]"></div>
    </div>

    <script type="module" src={smoothScroll}></script>
    <script type="module" src={bootAnimations}></script>
  </body>
</html>
